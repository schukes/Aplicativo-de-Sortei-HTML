<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8" />
    <title>Sorteio Corporativo - Sequencial Interativo</title>
    <script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.6.0/dist/confetti.browser.min.js"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700;900&display=swap');

        /* --- Variáveis de Tema (Dr Hasegawa) --- */
        :root { 
            --cor-primaria: #388E3C; /* Verde escuro */
            --cor-secundaria: #66BB6A; /* Verde claro */
            --cor-fundo: #E8F5E9; /* Verde muito claro para fundo */
            --cor-texto: #2E3A42;
            --cor-fundo-card: #fff;
            --cor-excluir: #E53935;
            --cor-aviso: #FF9800; /* Laranja para Reset */
            --cor-imprimir: #42A5F5; /* Azul para Imprimir */
        }

        body {
            font-family: 'Montserrat', sans-serif;
            background: linear-gradient(135deg, var(--cor-secundaria), var(--cor-primaria));
            color: var(--cor-texto);
            margin: 0;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        .container {
            background: var(--cor-fundo-card);
            padding: 30px 40px;
            border-radius: 18px;
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.2);
            max-width: 950px; 
            width: 100%;
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        h1 {
            margin-bottom: 5px;
            font-weight: 900;
            color: var(--cor-primaria);
            border-bottom: 3px solid var(--cor-secundaria);
            padding-bottom: 10px;
        }

        .content-wrapper {
            display: flex;
            gap: 30px;
        }

        .lista-secao {
            flex: 1;
            display: flex;
            flex-direction: column;
        }
        
        h2 {
            font-size: 1.2em;
            margin-bottom: 10px;
            color: var(--cor-primaria);
            font-weight: 700;
        }

        .lista-box {
            background: var(--cor-fundo);
            border: 1px solid var(--cor-secundaria);
            border-radius: 10px;
            padding: 10px 15px;
            font-size: 15px;
            color: var(--cor-texto);
            height: 200px;
            overflow-y: auto;
            text-align: left;
            user-select: none;
            flex-grow: 1;
        }

        .lista-box ul {
            list-style: none;
            margin: 0;
            padding: 0;
        }

        .lista-box li {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 8px 0;
            border-bottom: 1px dashed #ccc;
        }

        .lista-box li:last-child {
            border-bottom: none;
        }

        .btn-excluir {
            background: none;
            border: none;
            color: var(--cor-excluir);
            font-weight: 700;
            cursor: pointer;
            font-size: 1em;
            padding: 0 5px;
            opacity: 0.7;
            transition: opacity 0.2s;
        }

        .btn-excluir:hover {
            opacity: 1;
            color: #d32f2f;
        }

        .input-area {
            display: flex;
            gap: 10px;
            margin-top: 10px;
            justify-content: center;
        }

        .input-area input[type="text"] {
            flex: 1;
            padding: 12px 18px;
            font-size: 16px;
            border-radius: 8px;
            border: 1px solid #ccc;
            outline: none;
            transition: border-color 0.2s ease, box-shadow 0.2s ease;
        }

        .input-area input[type="text"]:focus {
            border-color: var(--cor-secundaria);
            box-shadow: 0 0 5px rgba(102, 187, 106, 0.8);
        }

        .input-area button,
        button.btn-acao {
            color: white;
            border: none;
            padding: 12px 25px;
            font-size: 16px;
            font-weight: 700;
            border-radius: 50px;
            cursor: pointer;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
            transition: background-color 0.3s ease, box-shadow 0.3s ease;
            outline: none;
            user-select: none;
        }
        
        .input-area button {
            background-color: var(--cor-secundaria);
        }

        .input-area button:hover {
            background-color: #5aa75e;
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.3);
        }

        .input-area button:disabled,
        button.btn-acao:disabled {
            background-color: #a5d6a7;
            cursor: not-allowed;
            box-shadow: none;
        }
        
        /* Controles de Sorteio de Quantidade Ocultado */
        .controles-sorteio { display: none; }

        button#btnSortear {
            background-color: var(--cor-primaria);
            margin-top: 15px;
            padding: 15px 40px;
            font-size: 20px;
            box-shadow: 0 8px 20px rgba(56, 142, 60, 0.5);
            flex-grow: 2;
        }

        button#btnSortear:hover {
            background-color: #317a35;
            box-shadow: 0 10px 25px rgba(49, 122, 53, 0.7);
        }
        
        .area-botoes-principais {
            display: flex;
            gap: 15px;
            flex-wrap: wrap; 
        }
        
        .area-botoes-principais button {
            margin-top: 15px;
            flex-grow: 1; 
        }

        #btnImprimirSorteados {
            background-color: var(--cor-imprimir);
        }
        
        #btnImprimirSorteados:hover {
            background-color: #3895e0;
        }

        #btnResetarSorteados {
            background-color: var(--cor-aviso);
        }
        
        #btnResetarSorteados:hover {
            background-color: #e68900;
        }
        
        #btnLimparSorteados {
            background-color: var(--cor-excluir);
        }
        
        #btnLimparSorteados:hover {
            background-color: #c62828;
        }


        .resultado {
            margin-top: 20px;
            font-size: 48px; 
            font-weight: 900;
            color: var(--cor-texto);
            background: #fff;
            padding: 15px 20px;
            border: 4px solid var(--cor-primaria);
            border-radius: 12px;
            box-shadow: 0 0 20px rgba(56, 142, 60, 0.6);
            min-height: 50px;
            text-align: center;
            user-select: none;
            animation: none;
        }
        
        .resultado.animacao-sorteio {
            font-size: 36px;
            animation: rollEffect 0.08s infinite alternate;
        }
        
        @keyframes rollEffect {
            0% { transform: scale(1.0); color: var(--cor-texto); }
            100% { transform: scale(1.03); color: var(--cor-primaria); }
        }

        .resultado.vencedor {
            font-size: 48px;
            background-color: #f1f8e9;
            border-color: var(--cor-primaria);
            color: var(--cor-primaria);
            animation: winnerGlow 1.5s ease-out 3;
        }
        
        @keyframes winnerGlow {
            0%, 100% { box-shadow: 0 0 20px rgba(56, 142, 60, 0.6); }
            50% { box-shadow: 0 0 35px rgba(102, 187, 106, 1); }
        }
        
        /* Estilos de Impressão */
        @media print {
            body > *:not(.print-container) {
                display: none;
            }
            .print-container {
                display: block !important;
                position: absolute;
                left: 0;
                top: 0;
                width: 100%;
                margin: 0;
                padding: 20px;
                background-color: white;
                color: black;
                font-family: 'Arial', sans-serif;
            }
            .print-container h1 {
                border-bottom: 2px solid #ccc;
                padding-bottom: 10px;
                color: black;
                font-size: 24pt;
            }
            .print-container ul {
                list-style: decimal;
                padding-left: 20px;
            }
            .print-container li {
                padding: 5px 0;
                border-bottom: 1px dotted #ccc;
                font-size: 14pt;
            }
        }
        
    </style>
</head>
<body>
    <div class="container">
        <h1>🏆 Sorteio Corporativo</h1>

        <div class="content-wrapper">
            <div class="lista-secao">
                <h2>Candidatos (Total: <span id="contadorCandidatos"></span>)</h2>
                <div class="lista-box" id="listaNomes">
                    </div>

                <div class="input-area">
                    <input type="text" id="novoNome" placeholder="Adicionar novo nome" />
                    <button id="btnAdicionar">Adicionar</button>
                </div>
            </div>

            <div class="lista-secao">
                <h2>Sorteados (Vencedores)</h2>
                <div class="lista-box" id="listaSorteados">
                    </div>
            </div>
        </div>
        
        <div class="controles-sorteio" style="display: none;">
             <label for="numVencedores">Sortear:</label>
             <input type="number" id="numVencedores" value="1" min="1" max="10" />
             <label for="numVencedores">vencedor(es) no total</label>
        </div>
        
        <div class="area-botoes-principais">
            <button id="btnSortear" class="btn-acao">Sortear Vencedor</button>
            <button id="btnContinuar" class="btn-acao" style="background-color: #007bff; display: none;">Continuar Sorteio</button> 
            <button id="btnImprimirSorteados" class="btn-acao">🖨️ Imprimir Vencedores</button>
            <button id="btnResetarSorteados" class="btn-acao">↩️ Voltar Nomes p/ Sorteio</button>
            <button id="btnLimparSorteados" class="btn-acao">Limpar Sorteados</button>
        </div>

        <div class="resultado" id="resultado">Aguardando sorteio...</div>
    </div>
    
    <div class="print-container" style="display: none;"></div>

    <script>
        // Funções de Persistência
        function carregarNomes(key, defaultValue) {
            try {
                const stored = localStorage.getItem(key);
                return stored ? JSON.parse(stored) : defaultValue;
            } catch (e) {
                console.error("Erro ao carregar dados do localStorage", e);
                return defaultValue;
            }
        }
        
        function salvarNomes(key, data) {
            try {
                localStorage.setItem(key, JSON.stringify(data));
            } catch (e) {
                console.error("Erro ao salvar dados no localStorage", e);
            }
        }
        
        // Inicialização de Dados com Persistência
        let nomes = carregarNomes('sorteio_candidatos', [
            "Felipe Martins", "Tiago Guedes", "Leticia", "Rodrigo", "Isadora",
            "Camila Beatriz", "Fernando", "Tiago Hasegawa", "Yasmim"
        ]);
        let nomesSorteados = carregarNomes('sorteio_sorteados', []);
        
        // --- Variáveis de Controle de Pausa ---
        let sorteioPausado = false; 
        let resolvePausa; 
        // --- FIM Variáveis de Controle de Pausa ---

        // Contexto de Áudio para geração de som
        const audioContext = new (window.AudioContext || window.webkitAudioContext)();
        
        // --- Referências de Elementos ---
        const listaNomesEl = document.getElementById('listaNomes');
        const listaSorteadosEl = document.getElementById('listaSorteados');
        const resultadoEl = document.getElementById('resultado');
        const btnSortear = document.getElementById('btnSortear');
        const novoNomeInput = document.getElementById('novoNome');
        const btnAdicionar = document.getElementById('btnAdicionar');
        const contadorCandidatosEl = document.getElementById('contadorCandidatos');
        const btnLimparSorteados = document.getElementById('btnLimparSorteados');
        const btnImprimirSorteados = document.getElementById('btnImprimirSorteados');
        const btnResetarSorteados = document.getElementById('btnResetarSorteados');
        const btnContinuar = document.getElementById('btnContinuar'); 


        // --- Funções de Som (Sem Alteração) ---
        
        function playClickBeep(delay = 0) {
            if (audioContext.state === 'suspended') return;

            const frequency = 700;
            const duration = 0.05;
            const oscillator = audioContext.createOscillator();
            const gainNode = audioContext.createGain();

            oscillator.type = 'sawtooth';
            oscillator.frequency.setValueAtTime(frequency, audioContext.currentTime + delay);

            gainNode.gain.setValueAtTime(0.3, audioContext.currentTime + delay);
            gainNode.gain.exponentialRampToValueAtTime(0.0001, audioContext.currentTime + delay + duration);

            oscillator.connect(gainNode);
            gainNode.connect(audioContext.destination);

            oscillator.start(audioContext.currentTime + delay);
            oscillator.stop(audioContext.currentTime + delay + duration);
        }

        function playWinnerMelody() {
            if (audioContext.state === 'suspended') return;
            
            const C5 = 523.25; const D5 = 587.33; const E5 = 659.25; 
            const G5 = 783.99; const A5 = 880.00; const C6 = 1046.5; 
            
            let time = audioContext.currentTime;
            
            function playNote(freq, start, dur, volume, waveType = 'square') {
                const osc = audioContext.createOscillator();
                const gain = audioContext.createGain();

                osc.type = waveType;
                osc.frequency.setValueAtTime(freq, start);
                
                gain.gain.setValueAtTime(0, start); 
                gain.gain.linearRampToValueAtTime(volume * 1.5, start + 0.03);
                gain.gain.linearRampToValueAtTime(volume, start + dur * 0.4);
                gain.gain.exponentialRampToValueAtTime(0.0001, start + dur);

                osc.connect(gain);
                gain.connect(audioContext.destination);

                osc.start(start);
                osc.stop(start + dur);
            }
            
            const quarter = 0.3; 
            const dottedQuarter = quarter * 1.5; 
            const eighth = quarter * 0.5; 
            const vol = 0.5;
            
            // Melodia
            playNote(C5, time, dottedQuarter, vol);
            playNote(D5, time + dottedQuarter, eighth, vol);
            time += (dottedQuarter + eighth);
            
            playNote(E5, time, dottedQuarter, vol);
            playNote(D5, time + dottedQuarter, eighth, vol);
            time += (dottedQuarter + eighth);

            playNote(C5, time, dottedQuarter, vol);
            playNote(D5, time + dottedQuarter, eighth, vol);
            time += (dottedQuarter + eighth);

            playNote(E5, time, dottedQuarter, vol);
            playNote(G5, time + dottedQuarter, eighth, vol);
            time += (dottedQuarter + eighth);
            
            playNote(C6, time, quarter, vol * 1.2, 'sine'); time += quarter;
            playNote(A5, time, quarter, vol * 1.2, 'sine'); time += quarter;
            playNote(G5, time, quarter, vol * 1.2, 'sine'); time += quarter;
            playNote(E5, time, quarter, vol * 1.2, 'sine'); time += quarter;
            
            playNote(C6, time, 1.2, vol * 1.5, 'triangle'); 
            playNote(E5, time, 1.2, vol * 1.5, 'triangle');
            playNote(G5, time, 1.2, vol * 1.5, 'triangle');
            
            time += 1.2;
        }


        // --- Funções de Lógica e Renderização ---

        function atualizarLista() {
            nomes.sort((a, b) => a.toLowerCase().localeCompare(b.toLowerCase()));
            const ulNomes = document.createElement('ul');
            nomes.forEach(nome => {
                const li = document.createElement('li');
                li.innerHTML = `
                    <span>${nome}</span>
                    <button class="btn-excluir" data-nome="${nome}">X</button>
                `;
                ulNomes.appendChild(li);
            });
            listaNomesEl.innerHTML = '';
            listaNomesEl.appendChild(ulNomes);
            
            // Adicionar event listeners para os botões de exclusão
            document.querySelectorAll('.btn-excluir').forEach(btn => {
                btn.addEventListener('click', (e) => {
                    const nomeParaExcluir = e.currentTarget.getAttribute('data-nome');
                    removerNome(nomeParaExcluir);
                });
            });

            // 2. Atualizar Lista de Sorteados
            const ulSorteados = document.createElement('ul');
            nomesSorteados.forEach((nome, index) => {
                const li = document.createElement('li');
                li.textContent = `${nomesSorteados.length - index}º: ${nome}`;
                ulSorteados.prepend(li);
            });
            listaSorteadosEl.innerHTML = '';
            listaSorteadosEl.appendChild(ulSorteados);
            
            // 3. Atualizar Contador e botões
            contadorCandidatosEl.textContent = nomes.length;
            
            // Determinar estados de bloqueio
            const isDrawing = resultadoEl.classList.contains('animacao-sorteio'); // TRUE durante a rolagem de nomes
            const isPaused = btnContinuar.style.display !== 'none'; // TRUE durante a pausa interativa

            // Botão Sortear principal é desabilitado se a lista estiver vazia ou se estiver em sorteio/pausa
            btnSortear.disabled = nomes.length === 0 || isDrawing || isPaused;
            
            // Botões auxiliares de Imprimir/Limpar/Resetar SÓ SÃO DESABILITADOS DURANTE A ROLAGEM
            btnImprimirSorteados.disabled = nomesSorteados.length === 0 || isDrawing;
            btnLimparSorteados.disabled = nomesSorteados.length === 0 || isDrawing;
            btnResetarSorteados.disabled = nomesSorteados.length === 0 || isDrawing;
            
            // Controles de Adição/Exclusão devem ser desabilitados durante a rolagem OU a pausa.
            btnAdicionar.disabled = isDrawing || isPaused;
            novoNomeInput.disabled = isDrawing || isPaused;
            document.querySelectorAll('.btn-excluir').forEach(btn => btn.disabled = isDrawing || isPaused);

            // Garantir que o botão Continuar esteja ativo durante a pausa
            if(isPaused) {
                btnContinuar.disabled = false;
            }
            
            // 4. Salvar no Local Storage
            salvarNomes('sorteio_candidatos', nomes);
            salvarNomes('sorteio_sorteados', nomesSorteados);
        }

        function sleep(ms) {
            return new Promise(resolve => setTimeout(resolve, ms));
        }
        
        // Função de Pausa Manual
        function waitForUserClick() {
            return new Promise(resolve => {
                resolvePausa = resolve;
                sorteioPausado = true;
                
                // Esconde o botão Sortear e mostra o Continuar
                btnSortear.style.display = 'none';
                btnContinuar.style.display = 'block';
                
                // Atualiza o texto do botão
                const restantes = nomes.length > 0 ? `(${nomes.length} restantes)` : `(Finalizar Sorteio)`;
                btnContinuar.textContent = `Continuar Sorteio ${restantes}`;
                
                // Os botões auxiliares estão liberados pela lógica de atualizarLista
                btnContinuar.disabled = false;
                
                atualizarLista(); 
            });
        }
        
        // Função para Continuar o Sorteio
        function continuarSorteio() {
            if (sorteioPausado && resolvePausa) {
                sorteioPausado = false;
                btnContinuar.style.display = 'none';
                btnSortear.style.display = 'block';
                
                // Reabilita controles de adição/exclusão antes de liberar a Promise
                document.querySelectorAll('.btn-acao').forEach(btn => btn.disabled = false);
                document.querySelectorAll('.btn-excluir').forEach(btn => btn.disabled = false);
                novoNomeInput.disabled = false;
                
                atualizarLista();

                // O fluxo de sorteio é reiniciado/continuado
                resolvePausa(); 
            }
        }
        
        function lancarConfetti() {
            const duration = 4 * 1000;
            const animationEnd = Date.now() + duration;
            const defaults = { startVelocity: 30, spread: 360, ticks: 60, zIndex: 0 };
            
            function randomInRange(min, max) {
                return Math.random() * (max - min) + min;
            }

            const interval = setInterval(function() {
                const timeLeft = animationEnd - Date.now();

                if (timeLeft <= 0) {
                    return clearInterval(interval);
                }

                const particleCount = 50 * (timeLeft / duration);
                const colors = ['#388E3C', '#66BB6A', '#ffffff']; 
                
                confetti(Object.assign({}, defaults, { particleCount, colors: colors, origin: { x: randomInRange(0.1, 0.3), y: randomInRange(0.7, 0.9) } }));
                confetti(Object.assign({}, defaults, { particleCount, colors: colors, origin: { x: randomInRange(0.7, 0.9), y: randomInRange(0.7, 0.9) } }));
            }, 250);
        }

        async function sortear() {
            if (audioContext.state === 'suspended') {
                audioContext.resume();
            }

            while (nomes.length > 0) {
                
                // Desabilitar todos os botões no INÍCIO do sorteio (exceto os auxiliares que serão liberados por atualizarLista)
                document.querySelectorAll('.btn-acao, .input-area button').forEach(btn => btn.disabled = true);
                novoNomeInput.disabled = true;
                document.querySelectorAll('.btn-excluir').forEach(btn => btn.disabled = true);

                // --- SORTEIO DE 1 VENCEDOR ---
                
                resultadoEl.textContent = `Sorteando o ${nomesSorteados.length + 1}º Vencedor...`;
                resultadoEl.classList.add('animacao-sorteio'); 
                resultadoEl.classList.remove('vencedor');

                const ciclos = 30 + Math.floor(Math.random() * 20);
                let indiceFinal = Math.floor(Math.random() * nomes.length);
                let nomeVencedor;
                
                // Simulação de rolagem
                for(let i = 0; i < ciclos; i++) {
                    playClickBeep(); 
                    let indiceRolagem = i % nomes.length;
                    resultadoEl.textContent = nomes[indiceRolagem];
                    let fatorDesaceleracao = Math.pow(i / ciclos, 3); 
                    let tempoEspera = 50 + fatorDesaceleracao * 300; 
                    await sleep(tempoEspera);
                }
                
                // Anuncia o Vencedor
                nomeVencedor = nomes[indiceFinal]; 
                resultadoEl.textContent = `🎉 ${nomeVencedor} 🎉`; 
                resultadoEl.classList.remove('animacao-sorteio');
                resultadoEl.classList.add('vencedor');
                
                playWinnerMelody(); 
                lancarConfetti();
                
                // Mover o vencedor
                const nomeIndex = nomes.indexOf(nomeVencedor); 
                nomesSorteados.unshift(nomes.splice(nomeIndex, 1)[0]);
                
                // Chama atualizarLista para liberar os botões auxiliares (Imprimir/Resetar/Limpar)
                atualizarLista(); 
                
                await sleep(4000); // Espera pela animação de confetes/música

                // --- PAUSA INTERATIVA ---
                
                // Se houver mais nomes, espera o clique para continuar
                if (nomes.length > 0) {
                    await waitForUserClick(); // Espera pelo clique no botão Continuar
                } 
                
            } // FIM DO WHILE (Sorteio Contínuo)

            // Finaliza quando a lista estiver vazia
            
            btnSortear.style.display = 'block'; // Volta o Sortear (embora desabilitado)
            btnContinuar.style.display = 'none';
            
            resultadoEl.classList.remove('vencedor');
            resultadoEl.textContent = "🥳 Sorteio Finalizado! Todos os candidatos foram sorteados.";
            
            // Reabilita todos os controles (Adicionar/Resetar/Imprimir/Limpar)
            document.querySelectorAll('.btn-acao, .input-area button').forEach(btn => btn.disabled = false);
            novoNomeInput.disabled = false;
            novoNomeInput.value = '';
            novoNomeInput.focus();

            atualizarLista();
        }

        // --- Outras Funções (Sem Alteração) ---
        
        function imprimirSorteados() {
            if (nomesSorteados.length === 0) {
                alert("Não há nomes sorteados para imprimir.");
                return;
            }

            let htmlContent = `
                <h1>Lista Oficial de Vencedores (${new Date().toLocaleDateString('pt-BR')})</h1>
                <ul>
            `;
            for (let i = nomesSorteados.length - 1; i >= 0; i--) {
                htmlContent += `<li>${nomesSorteados.length - i}º Vencedor: ${nomesSorteados[i]}</li>`;
            }
            htmlContent += `</ul>`;

            const printContainer = document.querySelector('.print-container');
            printContainer.innerHTML = htmlContent;
            
            window.print();
            
            printContainer.innerHTML = '';
        }
        
        function resetarSorteadosParaCandidatos() {
            if (nomesSorteados.length === 0) {
                alert("A lista de sorteados já está vazia. Não há nomes para resetar.");
                return;
            }

            if (confirm("ATENÇÃO: Deseja realmente mover TODOS os nomes sorteados de volta para a lista de Candidatos?")) {
                nomes = nomes.concat(nomesSorteados); 
                nomesSorteados = [];
                
                btnSortear.style.display = 'block';
                btnContinuar.style.display = 'none';
                resultadoEl.textContent = "Aguardando sorteio...";

                atualizarLista();
                alert("Os nomes sorteados foram resetados e estão de volta à lista de candidatos.");
            }
        }

        function capitalizeName(name) {
            return name.toLowerCase().split(' ').map(word => {
                if (word.length > 0) {
                    return word.charAt(0).toUpperCase() + word.slice(1);
                }
                return '';
            }).join(' ');
        }

        function adicionarNome() {
            let novoNome = novoNomeInput.value.trim();
            if (novoNome === '') {
                alert('Digite um nome válido para adicionar.');
                return;
            }
            
            novoNome = capitalizeName(novoNome); 

            if (nomes.some(n => n === novoNome) || nomesSorteados.some(n => n === novoNome)) {
                alert('Esse nome já está na lista de candidatos ou já foi sorteado.');
                return;
            }
            nomes.push(novoNome);
            atualizarLista();
            novoNomeInput.value = '';
            novoNomeInput.focus();
        }
        
        function removerNome(nomeParaExcluir) {
            if (confirm(`Tem certeza que deseja remover "${nomeParaExcluir}" da lista?`)) {
                nomes = nomes.filter(nome => nome !== nomeParaExcluir);
                atualizarLista();
            }
        }
        
        function limparSorteados() {
            if (nomesSorteados.length === 0) {
                alert("A lista de sorteados já está vazia.");
                return;
            }
            if (confirm("Você tem certeza que deseja limpar toda a lista de Vencedores Sorteados (eles serão perdidos se não estiverem na lista de candidatos)?")) {
                nomesSorteados = [];

                if(nomes.length > 0) {
                    btnSortear.style.display = 'block';
                    btnContinuar.style.display = 'none';
                    resultadoEl.textContent = "Aguardando sorteio...";
                } else {
                    resultadoEl.textContent = "Lista de candidatos vazia.";
                }

                atualizarLista();
                alert("Lista de sorteados limpa com sucesso!");
            }
        }

        // Event Listeners
        btnSortear.addEventListener('click', sortear);
        btnAdicionar.addEventListener('click', adicionarNome);
        btnLimparSorteados.addEventListener('click', limparSorteados);
        btnImprimirSorteados.addEventListener('click', imprimirSorteados); 
        btnResetarSorteados.addEventListener('click', resetarSorteadosParaCandidatos); 
        btnContinuar.addEventListener('click', continuarSorteio); 
        
        // Permitir adicionar nome ao pressionar Enter no input
        novoNomeInput.addEventListener('keyup', (e) => {
            if (e.key === 'Enter') {
                adicionarNome();
            }
        });

        // Inicializa lista na tela
        atualizarLista();
    </script>
</body>
</html>
